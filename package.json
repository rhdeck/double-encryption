{
  "name": "@raydeck/double-encryption",
  "version": "2.0.4",
  "description": "Double encryption manager",
  "main": "lib/index.js",
  "types": "lib/types",
  "repository": "https://github.com/rhdeck/double-encryption",
  "author": "Ray Deck",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc; typedoc --mode lib --theme minimal && yarn pack -f package.tgz",
    "watch": "node watch",
    "linecount": "wc -l src/*.js | sort",
    "prepublishOnly": "yarn build"
  },
  "devDependencies": {
    "@raydeck/livelink": "^1.5.2",
    "@types/node": "^14.0.20",
    "typedoc": "^0.17.0-3",
    "typescript": "^3.9.6"
  },
  "dependencies": {
    "@babel/runtime": "^7.8.4",
    "@raydeck/stream-crypto": "^2.2.2"
  },
  "files": [
    "src",
    "lib"
  ]
}
